import { defineModule } from "@/common/module";
import { ref } from "vue";
import { z } from "zod";
import { BlockInfoSchema, BlockDataSchema } from "@/common/types";





export const timeMachine = defineModule("timeMachine", {}, () => {
  // const savePoints = ref<SavePoint[]>([]);
});